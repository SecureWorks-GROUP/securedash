# Supabase functions in `securedash-booking-ui`

Production `ops-api` is intentionally not sourced from this repository.

Canonical source:

```text
/Users/marninstobbe/Projects/secureworks-site/supabase/functions/ops-api
```

Do not recreate or deploy `securedash-booking-ui/supabase/functions/ops-api`;
doing so can overwrite the live SecureSuite backend with a stale dashboard-only
copy.
