name: ops-api deploy disabled

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Canonical source guard
        run: |
          echo "Production ops-api deploys only from secureworks-site/supabase/functions/ops-api."
          echo "This securedash workflow is intentionally disabled to prevent stale overwrites."
